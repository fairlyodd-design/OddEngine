DROP YOUR REAL HOMIE MODEL FILES HERE.

Easy examples:
- homie.glb
- homie.vrm

Then inside the app, load them with paths like:
- /models/homie.glb
- /models/homie.vrm

This folder is inside the renderer's public assets, so these files become web paths automatically while the dev server is running.
