# v10.37.6_FairlyGodModeCommandDeckPolishAndTruthReasonPass

Purpose: polish the whole-OS FairlyGodMode command deck after v10.37.5b.

## Fixes

- Header title is FairlyGodMode instead of active panel title.
- Active panel appears as a secondary line.
- Panel rows explain good/warn/bad via a selected truth reason card.
- Panel rows include direct actions: Open, Focus, Why, Lock, Unlock, Reset.
- Reset only clears local FairlyGOD layout memory for that panel.
- FG/GOD launcher is smaller and more premium.

## Safety

No Trading logic changes. No CardGODMode internal rewrite. No Homie voice/memory/backend changes. No routing rewrite. No shell/rail ownership rewrite.

## Apply

```powershell
cd C:\OddEngine
.\RUN_APPLY_FAIRLYGODMODE_POLISH.bat
npm --prefix ui run build
npm run dev:desktop
```
