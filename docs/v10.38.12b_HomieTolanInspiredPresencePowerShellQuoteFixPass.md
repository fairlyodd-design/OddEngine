# v10.38.12b_HomieTolanInspiredPresencePowerShellQuoteFixPass

Fixes the PowerShell parser error from v10.38.12 by using quote-safe single-quoted replacement strings.

Apply:

```powershell
cd C:\OddEngine
.\RUN_APPLY_HOMIE_TOLAN_INSPIRED_PRESENCE_QUOTE_FIX.bat
npm --prefix ui run build
npm run dev:desktop
```

Safety: CSS and copy polish only. No backend, voice, Trading, CardGODMode, or shell changes.
