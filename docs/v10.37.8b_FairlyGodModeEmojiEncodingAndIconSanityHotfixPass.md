# v10.37.8b_FairlyGodModeEmojiEncodingAndIconSanityHotfixPass

## Mission

Fix mojibaked emoji/icon text in the FairlyGodMode deck after v10.37.8.

## What happened

The Workspace Modes feature works, but emoji/icon characters were decoded incorrectly by the Windows/PowerShell patch path, showing text like `ðŸ...` and `Ã¢`.

## Fix

- Replace mode emoji labels with ASCII-safe labels:
  - Legacy
  - Trade
  - Money
  - Studio
  - AM
  - PM
  - House
- Remove risky emoji rendering from the FairlyGodMode deck header/title spots.
- Save FairlyGodModeHUD.tsx and version.ts as UTF-8 without BOM.
- No behavior changes.

## Safety

No shell changes.
No Trading changes.
No CardGODMode changes.
No Homie backend/voice/memory changes.

## Apply

```powershell
cd C:\OddEngine
.\RUN_APPLY_FAIRLYGODMODE_EMOJI_HOTFIX.bat
npm --prefix ui run build
npm run dev:desktop
```
