# v10.38.8d_HomieCompanionLaneDirectAvatarOverridePass

## Mission

Force the companion popout avatar area to use the human-inspired Homie face.

## Adds/Fixes

- Adds `HomieDirectHumanAvatar`.
- Attempts direct insertion into current companion avatar stage classes.
- Fallback inserts avatar before the companion-lane copy if stage class is different.
- Adds marker: `data-homie-companion-avatar-override="v10.38.8d"`.
- Dims old robot/avatar contents inside the marked avatar stage.
- Keeps companion popout layout.
- Preserves Companion on / Hide buttons.
- Preserves Ready / Local bridge / Mic granted badges.
- Preserves memory/check-in/voice cards.

## Safety

No Homie backend rewrite.
No voice engine internals changed.
No Trading logic changes.
No CardGODMode internals changed.
No global shell layout changes.

## Apply

```powershell
cd C:\OddEngine
.\RUN_APPLY_HOMIE_COMPANION_DIRECT_AVATAR_OVERRIDE.bat
npm --prefix ui run build
npm run dev:desktop
```
