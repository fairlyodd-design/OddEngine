# v10.38.8f_HomieBuddyRiveStageReplacementPass

## Mission

Replace the actual Rive/robot avatar stage in `ui/src/components/HomieBuddy.tsx`.

## Fix

- Adds `HomieBuddyHumanAvatar` if missing.
- Replaces all `RiveHomie` JSX usages in HomieBuddy with `HomieBuddyHumanAvatar`.
- Handles self-closing and paired RiveHomie tags.
- Removes duplicate appended human cards/notes from previous sync attempts.
- Ensures there is only one human avatar instance if duplicates existed.
- Keeps source copy warm.

## Preserves

- Companion on / Hide buttons.
- Ready / Local bridge / Mic granted badges.
- Family legacy vault.
- Memory/check-in/voice cards.
- Companion layout.

## Apply

```powershell
cd C:\OddEngine
.\RUN_APPLY_HOMIE_BUDDY_RIVE_STAGE_REPLACEMENT.bat
npm --prefix ui run build
npm run dev:desktop
```
