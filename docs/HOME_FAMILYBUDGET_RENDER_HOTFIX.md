# v10.26.8a Home + Family Budget render hotfix

Fixes:
- Home no longer tries to render raw coupon/deal objects as React children.
- Family Budget now uses a safe Grocery Budget snapshot bridge for the new pressure board values.
