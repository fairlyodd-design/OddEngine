# v10.38.19d_HomieHoodHeadScaleAndNeckBlendFixPass

## Mission

Fix the pasted-head feeling by improving how Homie's head, hood, collar, and neck blend together.

## Improves

- Lowers the head slightly into the hood.
- Slightly reduces head scale.
- Reduces hoodie collar bulk around the chin/beard.
- Adds a cleaner neck transition.
- Makes the hood opening frame the head instead of swallowing it.
- Keeps the hoodie/jeans direction.
- Main Homie and HomieBuddy remain synced.

## Safety

- Visual asset/CSS only.
- No backend rewrite.
- No voice engine internals.
- No Trading changes.
- No CardGODMode changes.
- No shell changes.
- Routines/receipts/legacy sync preserved.

## Apply

```powershell
cd C:\OddEngine
.\RUN_APPLY_HOMIE_HOOD_HEAD_SCALE_NECK_BLEND_FIX.bat
npm --prefix ui run typecheck
npm --prefix ui run build
npm run dev:desktop
```

Then tag:

```powershell
git add .
git commit -m "v10.38.19d fix Homie hood head scale and neck blend"
git tag v10.38.19d-clean
git push origin main
git push origin v10.38.19d-clean
```
