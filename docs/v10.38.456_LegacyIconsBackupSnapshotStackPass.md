# v10.38.456 Legacy Icons Backup Snapshot Stack Pass

Extract contents directly into C:\OddEngine. The root must contain RUN_APPLY_LEGACY_ICONS_BACKUP_STACK.bat.

Apply:

```powershell
cd C:\OddEngine
dir RUN_APPLY_LEGACY_ICONS_BACKUP_STACK.bat
.\RUN_APPLY_LEGACY_ICONS_BACKUP_STACK.bat
npm --prefix ui run build
npm run dev:desktop
```
