# v10.25.29 — Mainline Truth and Ship Pass

This pass exists to make the repo and the build tell the same story again.

## Why this pass matters

The live GitHub repo now shows `main` with **51 commits**, `OddEngine_WORKFLOW.md` at the root, and a README that still renders with malformed literal `#` prefixes instead of clean markdown. That means the public repo has moved forward, but it still needs a truth-sync cleanup before it feels trustworthy as the ship line. citeturn534419view0

## What this pass changes

- normal clean `README.md`
- current-state docs that match the accepted product direction
- UI version markers set to `10.25.29`
- ship checklist for promoting recovery → main → checkpoint

## What this pass does not change

- no broad UI restyle
- no panel removals
- no new room layouts
- no new shell control system

## Accepted OS shape at ship time

- Home = heartbeat
- Homie = companion
- Trading = workstation
- Family = household flow
- Studio = creative engine

## Use

Copy these files into your real local tree, build, verify, commit on recovery, then merge to main.
