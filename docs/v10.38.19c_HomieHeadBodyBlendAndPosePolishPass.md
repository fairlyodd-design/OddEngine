# v10.38.19c_HomieHeadBodyBlendAndPosePolishPass

## Mission

Make the full-body Homie feel less assembled and more like one coherent 3D companion character.

## Improves

- Head sits more naturally into the hood.
- Neck/shoulder transition feels less pasted-on.
- Hoodie collar frames the face better.
- Arms/hands move into a more natural relaxed pose.
- Jeans get a cleaner character taper.
- Shoes are smaller and softer.
- Main stage avatar is slightly larger and better centered.
- Companion popout avatar is better framed.

## Safety

- Visual asset/CSS only.
- No backend rewrite.
- No voice engine internals.
- No Trading changes.
- No CardGODMode changes.
- No shell changes.
- Routines/receipts/legacy sync preserved.

## Apply

```powershell
cd C:\OddEngine
.\RUN_APPLY_HOMIE_HEAD_BODY_BLEND_POSE_POLISH.bat
npm --prefix ui run typecheck
npm --prefix ui run build
npm run dev:desktop
```

Then tag:

```powershell
git add .
git commit -m "v10.38.19c polish Homie head body blend and pose"
git tag v10.38.19c-clean
git push origin main
git push origin v10.38.19c-clean
```
