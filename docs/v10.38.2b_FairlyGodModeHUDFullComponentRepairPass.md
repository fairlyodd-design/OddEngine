# v10.38.2b_FairlyGodModeHUDFullComponentRepairPass

## Mission

Repair the v10.38.2 build break by replacing FairlyGodModeHUD.tsx as a clean full component.

## Why

The v10.38.2 regex patch corrupted FairlyGodModeHUD.tsx near the hook section, causing:

`Expected "}" but found "s"` around loose `u s e E` text.

## Fix

- Full replacement of `ui/src/components/FairlyGodModeHUD.tsx`.
- Keeps:
  - Doctor tab
  - Panels tab
  - Modes tab
  - Receipts tab
  - Homie Operator Bridge tab
  - Legacy tab
  - Safety tab
- Appends repair CSS safely.
- Updates version marker.

## Safety

No App.tsx surgery.
No shell layout rewrite.
No Trading logic rewrite.
No CardGODMode internals rewrite.
No Homie backend/voice/memory rewrite.

## Apply

```powershell
cd C:\OddEngine
.\RUN_APPLY_FAIRLYGODMODE_HUD_REPAIR.bat
npm --prefix ui run build
npm run dev:desktop
```
