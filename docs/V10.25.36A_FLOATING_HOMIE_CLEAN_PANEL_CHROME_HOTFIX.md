# v10.25.36a — Floating Homie + Clean Panel Chrome Hotfix

This hotfix does two things:

1. moves Homie back into a more floating host position instead of anchoring it in the lower-right corner
2. hides the panel chrome that brought back the `- + pin` controls and widget action clutter

## Files
- `ui/src/styles.css`

## Notes
- This is intentionally tiny and visual-only.
- It does not rewrite shell logic or panel contents.
- On narrower screens, Homie drops back down so it does not cover too much content.
