# v10.37.5b_FairlyGodModeApplyScriptHotfixPass

Fixes the PowerShell parser failure in v10.37.5 caused by escaped quotes inside a double-quoted replacement string.

## Scope

- Adds `FairlyGodModeHUD.tsx`
- Adds `FairlyGodModeHUD.css`
- Safely patches `ui/src/App.tsx`
- Updates `APP_VERSION` to `10.37.5b`

## Apply

```powershell
cd C:\OddEngine
.\RUN_APPLY_FAIRLYGODMODE_WHOLE_OS_HOTFIX.bat
npm --prefix ui run build
npm run dev:desktop
```

## Safety

Does not modify Trading, CardGODMode internals, Homie voice, Homie memory, backend, or routing.
