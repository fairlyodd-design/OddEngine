# v10.26.7b Grocery Retail Auth and Live Price Hooks Pass

This pass adds a local-first scaffold for the next real grocery savings lane:

- retailer auth readiness toggles
- live hook toggles per connector
- Deal Scout local rescoring mode
- connector capability badges
- richer per-item lane reasoning

Important: this pass does not claim live retailer scraping or guaranteed best prices. It adds the app structure for auth-ready and hook-ready workflows while keeping the current local planning engine stable.
