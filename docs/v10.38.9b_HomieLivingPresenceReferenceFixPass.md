# v10.38.9b_HomieLivingPresenceReferenceFixPass

## Fix

Repairs the blank-screen regression from v10.38.9:

`ReferenceError: livingPresenceState is not defined`

## Cause

`data-presence-state={livingPresenceState}` was applied to the HomieBuddy avatar, but the const did not land in scope before `avatarContents` in the local drifted file.

## What this does

- Ensures `getHomieLivingPresenceState` helper exists.
- Ensures `getHomieVoiceWarmthLine` helper exists.
- Inserts:

```ts
const livingPresenceState = getHomieLivingPresenceState(...);
const voiceWarmthLine = getHomieVoiceWarmthLine(...);
```

immediately before:

```ts
const avatarContents = (
```

- Adds a defensive fallback: `livingPresenceState || "idle"`.

## Safety

- No Homie backend rewrite.
- No voice engine internals changed.
- No Trading changes.
- No CardGODMode changes.
- No shell surgery.

## Apply

```powershell
cd C:\OddEngine
.\RUN_APPLY_HOMIE_LIVING_PRESENCE_REFERENCE_FIX.bat
npm --prefix ui run build
npm run dev:desktop
```
