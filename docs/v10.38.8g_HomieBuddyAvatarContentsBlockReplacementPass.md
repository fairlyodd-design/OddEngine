# v10.38.8g_HomieBuddyAvatarContentsBlockReplacementPass

Replaces the exact local `avatarContents` block in `ui/src/components/HomieBuddy.tsx`.

The trace showed the right companion avatar is the local block at lines 3086-3117, rendered inside `homieRebuildAvatarWrap` at lines 3148-3153. fileciteturn60file0

Apply:

```powershell
cd C:\OddEngine
.\RUN_APPLY_HOMIE_BUDDY_AVATAR_CONTENTS_REPLACEMENT.bat
npm --prefix ui run build
npm run dev:desktop
```

Preserves buttons, badges, consent row, family legacy vault, check-in, memory, voice cards, and layout.
