# v10.38.2c_ShellSummaryDuplicateHeaderCalmPass

## Mission

Calm the global ShellSummary so it no longer looks like a duplicate panel header on every panel.

## Problem

After the icon/emoji cleanup, the shell summary began rendering labels like:

- HOME Home
- BRAIN OddBrain
- BUDGET Family Budget

It also visually competed with each panel's own header.

## Fix

- Removes `meta.icon` from ShellSummary title lines in App.tsx.
- Turns the shell hero into a slim context strip.
- Keeps mission/note/goal stats visible but smaller.
- Keeps command bar and panel behavior intact.
- Preserves ShellSummary functionality.

## Safety

No Trading logic changes.
No CardGODMode internal changes.
No Homie backend/voice/memory changes.
No routing rewrite.
No FairlyGodMode component rewrite.

## Apply

```powershell
cd C:\OddEngine
.\RUN_APPLY_SHELL_SUMMARY_CALM.bat
npm --prefix ui run build
npm run dev:desktop
```
