# v10.38.8e_HomieCompanionSourceTraceAndAvatarSlotFixPass

## Mission

Patch the actual source of the right companion lane:

`ui/src/components/HomieBuddy.tsx`

## Why

The user traced the current popout strings to `HomieBuddy.tsx`:

- Companion on
- Mic granted
- Memory: local
- A calmer Homie lane
- Family legacy vault

Previous passes targeted `Homie.tsx`, but the right popout is rendered by `HomieBuddy.tsx`.

## Fix

- Adds `HomieBuddyHumanAvatar`.
- Replaces `RiveHomie` avatar instances in `HomieBuddy.tsx` only.
- Falls back to inserting above the assistant section title if no Rive tag is found.
- Warms copy to "Human Homie companion lane".
- Preserves:
  - Companion on / Hide
  - Ready / Local bridge / Mic granted
  - Memory/check-in/voice cards
  - Layout and shell

## Safety

No Homie backend rewrite.
No voice engine internals changed.
No Trading logic changes.
No CardGODMode internals changed.
No global shell layout changes.

## Apply

```powershell
cd C:\OddEngine
.\RUN_APPLY_HOMIE_COMPANION_SOURCE_AVATAR_FIX.bat
npm --prefix ui run build
npm run dev:desktop
```
