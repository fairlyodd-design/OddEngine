# v10.38.20_HomieReferenceCompositeAvatarPass

## Mission

Stop micro-adjusting the hand-drawn body and directly composite the references the way requested.

## What this does

- Uses the user-provided hoodie-character reference as the body/silhouette direction.
- Uses the FairlyOdd/Homie reference head as the face/head direction.
- Creates a repo-safe composite full-body avatar asset.
- Syncs main Homie + HomieBuddy.
- Keeps existing fallback file names so prior stages still work.

## Adds

```txt
ui/public/homie/homie-composite-hoodie-avatar.png
ui/public/homie/homie-composite-hoodie-avatar-512.png
ui/public/homie/homie-composite-hoodie-avatar-256.png
```

## Safety

- Visual asset/CSS only.
- User-provided references only.
- No backend rewrite.
- No voice engine internals.
- No Trading changes.
- No CardGODMode changes.
- No shell changes.

## Apply

```powershell
cd C:\OddEngine
.\RUN_APPLY_HOMIE_REFERENCE_COMPOSITE_AVATAR.bat
npm --prefix ui run typecheck
npm --prefix ui run build
npm run dev:desktop
```

Then tag:

```powershell
git add .
git commit -m "v10.38.20 composite Homie hoodie avatar from references"
git tag v10.38.20-clean
git push origin main
git push origin v10.38.20-clean
```
