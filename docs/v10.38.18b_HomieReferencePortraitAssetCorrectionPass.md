# v10.38.18b_HomieReferencePortraitAssetCorrectionPass

## Mission

Correct the weird v10.38.18 generated Homie portrait by using the provided reference-style image direction as the actual portrait asset source.

## What this does

- Replaces the generated alien-looking portrait with a cleaned/cropped reference portrait asset.
- Keeps the asset-backed portrait approach.
- Keeps CSS-built Homie underneath as fallback.
- Keeps main Homie and HomieBuddy synced.
- Preserves routines, receipts, legacy sync, voice badges, and companion layout.

## Files copied into the app

```txt
ui/public/homie/homie-reference-portrait.png
ui/public/homie/homie-reference-portrait-512.png
ui/public/homie/homie-reference-portrait-256.png
```

## Safety

- Visual asset/CSS correction only.
- No backend rewrite.
- No voice engine internals.
- No Trading changes.
- No CardGODMode changes.
- No shell changes.

## Apply

```powershell
cd C:\OddEngine
.\RUN_APPLY_HOMIE_REFERENCE_PORTRAIT_CORRECTION.bat
npm --prefix ui run typecheck
npm --prefix ui run build
npm run dev:desktop
```

Then tag:

```powershell
git add .
git commit -m "v10.38.18b correct Homie reference portrait asset"
git tag v10.38.18b-clean
git push origin main
git push origin v10.38.18b-clean
```
