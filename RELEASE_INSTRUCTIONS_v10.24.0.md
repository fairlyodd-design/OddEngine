# Release instructions for v10.24.0

This package updates local metadata and documentation to align to v10.24.0.

I could not create the Git tag or push to GitHub from this environment.

Run these commands in your local repo after verifying the files:

```bash
git add .
git commit -m "chore: align metadata/docs to v10.24.0"
git tag v10.24.0
git push origin main --tags
```
