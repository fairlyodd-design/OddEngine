# Patch notes — v10.36.17b

- Rewrites `.github/workflows/ci-ui-build.yml`
- Keeps Node 24 action path
- Uses `npm install` instead of strict `npm ci --include=dev`
- Adds shell trace/failure breadcrumbs
- No app code changes
