# Patch Notes — v10.36.15

## Homie Companion Life Coach

- Adds a companion/life-coach reply lane.
- Voice transcripts that are not clear OS commands now get companion-style responses instead of always being routed to Brain.
- Keeps all existing command behavior for `open`, `run`, `build`, `probe`, `start`, etc.
- Adds quick check-in, focus, grounding, and legacy prompts.
- Preserves external/local voice bridge support and existing mic diagnostics.
- Adds soft companion visual polish.

## Untouched

- Trading
- CardGODMode
- Home
- Writers/Studio
- CI workflow
- Global drag/drop
