v10.26.20h2_TrueDragHandleAndNonBlockingInteractionPass

This pass makes widget drag header-based only, keeps resize corner-only, removes the blocking drag/resize overlay label, and tightens CardGODMode interaction rules so content stays clickable.
