# v10.26.14p checklist like I'm 5

1. Copy the overlay zip on top of your current project.
2. Start the desktop app again.
3. Homie should now open in a normal detached window with safer size/position.
4. If you still had a weird skinny/off-screen companion before, this pass resets that startup memory once.
5. In Blender, use:
   - `scripts/lilhomie_blender_armature_helper.py`
   - `scripts/lilhomie_blender_viseme_shape_keys_helper.py`
6. Export your custom `ui/public/models/lilhomie.glb`.
7. Keep `ui/public/models/lilhomie.manifest.json` aligned with your final clip names.
