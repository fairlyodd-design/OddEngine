# v10.26.20f Camera bridge + motion pass

## What changed
- Cameras panel can now save a live bridge URL
- RTSP feeds can point at a browser relay seam
- motion events show up in the panel instead of feeling invisible
- bridge checks create real system runs / events / receipts

## What to test
1. Open Cameras
2. Set bridge URL to `http://127.0.0.1:8898`
3. Click **Check bridge**
4. Confirm bridge status updates
5. Trigger motion on a camera tile
6. Confirm the event shows in Alerts + views
7. Confirm RTSP cameras show relay-active when auto relay is on

## Real-life next move
Run the starter backend, then replace `/camera/hls` or `/camera/mjpeg` with a real go2rtc or ffmpeg relay path.
